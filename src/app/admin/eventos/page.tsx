import { EventCenter } from '@/components/events/EventCenter'

// Administração › Eventos (EVENT-001). Estado VISUAL apenas — catálogo estático, nenhum evento publicado.
export default function Page() {
  return <EventCenter />
}
